// //10. Adding Comments: Choose two of the programs you’ve written, and add at least one comment to each. If you don’t have anything specific to write because your programs are too simple at this point, just add your name and the current date at the top of each program file. Then write one sentence describing what the program does.

// let a : number = 20;
// let b : number = 10;

// // I am using Template Literals. basically template literals are being used to interpolation (embeded expression or anything into strings)

// console.log(`${a}+${b}= ${a + b}`);

// //subtraction

// console.log(`${a}-${b}=${a - b}`);

// //Multiplication

// console.log(`${a}*${b}=${a * b}`);

// //Division

// console.log(`${a}/${b}=${a / b}`);

let names:string[]=["Zeeshan Sharif", "Naveed Ahmed", "Muhammad Tahir"];
//Using For loop

for (let i=0; i < names.length; i++){
console.log(names[i]);
}
